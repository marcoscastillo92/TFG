{
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.162.0"
  },
  "devDependencies": {
    "vite": "^5.2.2"
  }
}
